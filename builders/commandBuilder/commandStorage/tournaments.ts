import { ISlashCommandItem } from '../commandBuilder'

/** Saved version of the Tournament command */
export const tournamentCommand: ISlashCommandItem = {
    commandName: 'tournament',
    commandDescription: 'se rocket league tournaments',
}
