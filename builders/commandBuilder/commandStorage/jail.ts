import { ISlashCommandItem } from '../commandBuilder'

/** Saved version of the Jail command */
export const jailCommand: ISlashCommandItem = {
    commandName: 'jail',
    commandDescription: 'se hvem som sitter i fengsel',
}
