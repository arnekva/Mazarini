import { ISlashCommandItem } from '../commandBuilder'

/** Saved version of the Stopp command */
export const refreshCommand: ISlashCommandItem = {
    commandName: 'refresh',
    commandDescription: 'trigg en refresh',
}
