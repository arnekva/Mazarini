import { ISlashCommandItem } from '../commandBuilder'

/** Saved version of the Tournament command */
export const calendarCommand: ISlashCommandItem = {
    commandName: 'julekalender',
    commandDescription: 'en hyggelig nedtelling til jul',
}
